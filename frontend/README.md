# Quiz Frontend

## Setup

1. Install dependencies:

	npm install

2. Create env file:

	copy .env.example .env

3. Start app:

	npm run dev

Frontend runs on http://localhost:5173 by default.

## Admin Dashboard

- Open the start screen and click `Open Admin Dashboard`.
- Dashboard fetches `/api/admin/submissions` and auto-refreshes every 15 seconds.
